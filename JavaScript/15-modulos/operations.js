const sumar = (num1, num2) => sum1 + sum2
const restar = (num1, num2) => sum1 - sum2
const multiplicar = (num1, num2) => sum1 * sum2
const dividir = (num1, num2) => sum1 / sum2



export {
    sumar,
    restar, 
    multiplicar,
    dividir
}