const sumar = (a, b) => {
    return a + b;
}

const restar = (a, b) => {
    return a - b;
}

const multiplicar = (a, b) => {
    return a - b;
}

module.exports = {
    sum: sumar,
    res: restar,
    mul: multiplicar,
};